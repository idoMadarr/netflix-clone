const initState = {
  username: '',
  token: '',
  emailSign: '',
};

const reducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
